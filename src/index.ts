import Strategy from "./strategy";

/**
 * Main export file for the OAuth2 Strategy module.
 *
 * This file imports the Strategy from './strategy' and exports it for use in other parts of the application.
 * It serves as an entry point for the module, making it easier to import the Strategy where needed.
 */

export = Strategy;